# Runtime / agent
.domi/
.superpowers/

# Build artifacts
target/
**/target/
node_modules/
dist/
build/

# Astro auto-generated types and content maps (created by `astro check` and `astro dev`)
.astro/
**/.astro/
packages/astro/.astro/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Env
.env
.env.local

# Knowledge graph (generated by `npm run graph`, gitignored so each
# agent regenerates locally instead of sharing a stale map).
graphify-out/

# Rust — Cargo.lock is gitignored for both library-only and binary builds (Phase 2c-α/β/γ policy).
Cargo.lock

docs/superpowers/plans/2026-07-10-domicile-rename.md
docs/superpowers/specs/2026-07-10-domicile-rename-design.md
