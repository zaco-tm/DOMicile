# select

Glass-styled dropdown.

## Usage

```html
<select class="domi-select">
  <option>Option A</option>
</select>
```

## Variants

- `domi-select--sm` / `--lg` — size
- `domi-select--error` — error state
